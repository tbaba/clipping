%h2 Listing webpages

= form_for @webpage do |f|
  %dl
    %dt= f.label :title
    %dd= f.text_field :title
    %dt= f.label :url
    %dd= f.url_field :url
    %dt= f.label :description
    %dd= f.text_area :description

  %nav
    %p= f.submit 'Register'

- @webpages.each do |webpage|
  %article
    %h3= webpage.title
    %p= link_to webpage.url, webpage.url
    = simple_format webpage.description

    %aside
      = link_to 'Show', webpage
      |
      = link_to 'Edit', edit_webpage_path(webpage)
      |
      = link_to 'Destroy', webpage, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Webpage', new_webpage_path
